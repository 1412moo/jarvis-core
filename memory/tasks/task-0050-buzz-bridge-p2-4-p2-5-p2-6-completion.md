# task-0050-buzz-bridge-p2-4-p2-5-p2-6-completion

- id: `task-0050-buzz-bridge-p2-4-p2-5-p2-6-completion`
- title: `Discord 승인 경계 강화 P2-4 / 아웃바운드 Buzz 상태 알림 P2-5 / Relay 실통합 검증 P2-6 완료 (provenance 기록)`
- status: `DONE`
- repo: `jarvis-core`
- created_at: `2026-09-03 UTC`
- updated_at: `2026-09-03 UTC`
- summary: `이 문서는 \`memory/tasks/task-0049-buzz-bridge-p2-2-p2-3-completion.md\`(P2-2/P2-3, commit \`5cf9930\`/\`11e74e6\`) 이후 진행된 P2-4/P2-5(commit \`b270f63c6b3a1e3727ecdfbd7b75a568654ce030\`)와 P2-6(코드 변경 없는 Relay 실통합 검증)의 provenance를 기록한다. task-0049는 "task-0038 §6의 남은 단계는 전부 미착수"라고 기록했는데 그 이후 ③(승인 게시 흐름)의 아웃바운드 절반이 실제로 구현·검증되었으므로 이 문서가 그 변화를 메운다. task-0048/task-0049의 기존 내용은 수정하지 않았다.

**P2-4 완료** — commit \`b270f63\`("feat(discord): Owner-only privileged commands (P2-4) + outbound Buzz status notification (P2-5)"). \`adapters/discord/bot_minimal.py\`의 \`on_message\`에서 \`_run_command(content)\` **호출 직전 한 곳에만** 인가 게이트를 추가했다. 위치가 자연어 분기가 \`content\`를 구체 명령으로 재작성한 **뒤**이므로 NL 경로가 합성한 \`/approve ...\`도 타이핑된 것과 동일하게 게이트된다(\`orchestrator/discord-nl-intent/intent_dispatcher.py\`가 approve_task intent에서 그 문자열을 만들 수 있다). privileged 명령은 \`/approve\`/\`/run\`/\`/retry\`/\`/task\` 4종이며 — \`/approve\`만 막으면 \`/run\`·\`/retry\`가 같은 \`_run_execution_flow\` subprocess 실행에 도달하고 \`/task\`는 파일 생성 권한이므로 Owner가 4종을 동일 권한군으로 확정했다 — read-only 6종(\`/status\`,\`/report\`,\`/help\`,\`/plan\`,\`/review-task\`,\`/retro\`)은 기존대로 개방된다. Owner allowlist는 환경변수 \`JARVIS_OWNER_DISCORD_USER_IDS\`(comma-separated, 숫자 snowflake만 채택)에서 오고, 미설정이거나 유효 id가 하나도 없으면 \`_validate_required_env()\`가 기동을 거부한다(기존 \`DISCORD_BOT_TOKEN\` fail-closed 패턴과 동일). \`_authorize_command\`는 전체를 try/except로 감싸 **allow로 되돌아가는 경로가 존재하지 않으며**, 거부 사유는 일반화된 \`unauthorized\` 하나뿐이라 Owner가 누구인지도 allowlist 설정 여부도 노출하지 않는다. Owner id 값은 로그·에러에 절대 나타나지 않고 환경변수 *이름*만 나타난다. \`_run_command\` 시그니처와 승인 파이프라인(\`_build_approve_draft\`/\`_build_approve_writer_input\`/\`_build_approve_writer_result\`/\`_run_approve_parse\`/\`_apply_task_status_transition\`/\`_run_execution_flow\`)은 무수정이라 경계가 두 곳으로 갈라지지 않는다. \`_run_command\`가 \`str.startswith()\`로 라우팅하므로 첫 토큰이 privileged 이름으로 시작만 해도(\`/approvex\`, \`/taskfoo\`) privileged로 분류해 거부한다. 신규 self-check 8건 추가.

**P2-5 완료** — 동일 commit \`b270f63\`, 신규 파일 \`orchestrator/buzz-bridge/publish_notification.js\`(197줄). 확정된 승인 결과(\`result_type == "approve_file_write_result"\` **그리고** \`applied is True\`)만 전용 채널 \`jarvis-task-status-notifications\`에 **아웃바운드 단방향** 발행한다. 훅은 \`on_message\`에서 Owner에게 승인 결과 reply를 **보낸 뒤** \`asyncio.to_thread\`로 실행되므로 relay가 느려도 승인 통지가 지연되지 않는다. 발행 payload는 상태 전이 사실(\`task_id\`/\`from\`/\`to\`/\`execution_status_transition_applied\`/\`ts\`)만 담고 approve·reject·grant·deny 같은 명령형 어휘를 싣지 않는다. 이벤트 태그는 \`buildNotificationTags()\` 고정 리터럴 2개(\`jarvis-task\`, \`jarvis-notification\`)가 유일한 출처라 stdin 입력으로 \`p\` 멘션 태그나 \`jarvis-run\` 태그를 주입할 수 없고, 그 결과 \`bridge.js\`의 \`passesInboundGate\`를 구조적으로 통과하지 못한다. 전용 채널을 쓰는 것은 \`bridge.js\`의 \`#h\` 구독 필터에 아예 걸리지 않게 하는 두 번째 방어층이다(같은 채널을 쓰면 알림마다 ANOMALY 로그가 쌓여 보안 로그가 무뎌진다). publisher는 \`subscribeLive\`를 호출하지 않고 task 파일을 읽거나 쓰지 않으며 \`orchestrator.js\`/\`bridge.js\`를 호출하지 않는다. 실행은 전용 상수 \`NOTIFICATION_PUBLISHER_COMMAND\`를 쓰고 \`EXECUTION_SCRIPT_WHITELIST\`는 재사용하지도 확장하지도 않았다(알림이 \`/run\`·\`/retry\`로 도달 가능해지는 것을 막기 위함). 승인 결과 격리: publish 실패는 \`/approve\` 성공/실패를 바꾸지 않고 \`result\` 객체를 변형하지도 않으며, "승인 처리는 완료되었으나 Buzz 알림 발행에 실패했다"는 별도 메시지로만 보고한다. 역방향으로 publish 성공을 승인 성공의 근거로 삼지도 않는다. 이는 P2-2의 \`appendRunRecord\` 실패 처리 계약("A failed append must never look like a failed run")을 그대로 상속한 것이다. 신규 self-check 8건 + buzz-bridge 오프라인 테스트 4건 추가(기존 31건은 무변경, 단일 hunk 순수 삽입).

**P2-6 완료 — 코드 변경 0의 실통합 검증** — 2026-09-02 수행, 커밋 없음(검증 전용이라 git history로 복원 불가하므로 이 문단이 유일한 기록이다). 기존 \`deploy/compose.yml\` + 기존 \`deploy/.env\`로 relay stack을 공식 절차대로 기동해 relay/postgres/redis/minio 전부 \`healthy\`, minio-init \`Exited(0)\`, \`/_readiness\` HTTP 200(11ms)을 확인했다. NIP-42 handshake는 challenge 64자 수신 후 auth 35ms 성공. 실제 round-trip: P2-4 게이트에서 Owner allow/비-Owner deny를 모두 확인한 뒤 \`_run_command("/approve ...")\`로 authoritative 전이 \`NEEDS_APPROVAL -> DOING\`(\`applied=true\`, \`error=false\`)을 발생시키고, 그 결과로 publisher subprocess가 실행되어 relay가 \`OK true\`를 반환했다. 관측은 **publisher와 다른 개인키(jarvis-agent-claude)로 재인증한 독립 observer**가 relay에서 직접 읽어 수행했다(자기확인이 아님). 관측된 live event id \`373023ff0f6aa0ff9fe80ef104d51614b4331abfafee37a93c4c0f69a14858c8\`, 채널 \`jarvis-task-status-notifications\`(id \`ea8fe877-e7c8-4d78-932b-e164cc623f13\`), kind \`9\`, 서명 검증 통과, 태그는 \`["h", <채널 id>]\`/\`["jarvis-task","task-9999-p26-relay-roundtrip"]\`/\`["jarvis-notification","task_status"]\` 3개뿐으로 \`p\` 태그 없음·\`jarvis-run\` 태그 없음, content는 \`{"notification_type":"task_status","from":"NEEDS_APPROVAL","to":"DOING",...}\`로 승인 어휘 없음. 그 **live event를 실제 \`passesInboundGate\`에 투입해 \`ok:false\`, reason \`"no [p, own_pubkey] mention tag"\`** 를 확인했고 \`matches_bridge_subscription_filter:false\`로 bridge 구독 필터에도 걸리지 않음을 확인했다. 승인 결과 객체는 publish 전후 스냅샷이 동일해 무변조. 검증에 쓴 task fixture는 저장소 **바깥** 임시 디렉터리에서만 만들었고 검증 스크립트도 전부 저장소 밖에 두었다가 제거했다. 종료는 \`docker compose down\`(\`-v\` 미사용)으로 수행해 buzz 볼륨 4개를 보존했다. 저장소는 tracked 변경 0으로 유지됐다.

**G1/G2/G3 재확인** — **G1 PARTIAL**(Windows self-host relay CORS #3490/#2872는 여전히 OPEN이나 P2-5 경로는 headless WebSocket 전용이라 무관, 변화 없음) / **G2 PASS**(WebSocket 직결 Agent Bridge 경로 무변경, P2-6에서 실 relay 왕복으로 재확인) / **G3 FAIL이지만 비차단**(Buzz의 승인 게이트 executor는 여전히 미완성이나, P2-5는 Buzz의 \`request_approval\`/\`kind:46011\`/grant-deny API를 어디서도 호출하지 않는 아웃바운드 전용이고 승인 판정은 100% Jarvis가 소유하므로 기존 원칙으로 계속 충족). 세 판정 모두 이 증분으로 바뀌지 않았다.

**아직 하지 않은 것 / 이 문서가 승인하지 않는 것** — task-0038 §6 통합 순서 중 ②(\`@jarvis\` 멘션 → 기존 \`intake_parser\` 재사용 → task 이벤트 생성), ④(Reviewer/QA를 별도 키의 Buzz agent로 등록 + git worktree 격리), ⑤(Discord intake 최소 3개월 병행)는 **전부 미착수이며 착수에 별도의 명시적 Owner 결정이 필요하다 — 이 문서는 그 어떤 것도 승인하지 않는다.** ③(승인 게시 흐름)은 아웃바운드 절반만 완료됐고 **Buzz -> Jarvis 인바운드 승인 입력은 Owner가 명시적으로 범위 밖으로 결정**했으므로 ③ 전체가 완료된 것이 아니다. 감사 기록에 승인자 ID를 남기는 작업은 P2-4에서 의도적으로 분리한 별건이며 미착수다(현재 \`_write_execution_review_metadata\`에 행위자 필드가 없어 사후 추적이 불가하다). Codex/agy agent bridge 확장, bridge/relay 프로세스 supervisor 도입, Director Dashboard v0.1B 재개도 마찬가지로 미착수이고 이 문서가 승인하지 않는다. Phase 1 잔여 항목 \`task-0042-role-based-signing-keys\`(역할별 Ed25519 서명키)와 \`task-0044-audit-hash-chain\`(감사 해시체인)은 **여전히 TODO**이며, 두 항목은 §6 착수 승인 없이도 진행 가능한 선택적 잔여 항목이라는 기존 위치를 그대로 유지한다. 특히 task-0042는 §6 ④가 요구하는 "별도 키의 Buzz 에이전트"의 선행조건이라는 점만 사실로 기록해 두되, 그것이 ④ 착수를 승인한다는 뜻은 아니다. 이 문서는 다음 증분의 기준점(baseline)으로 쓰인다.`
- source_command: `Owner 직접 지시 (2026-09-03): "provenance 보류를 해제한다. 이번 작업은 문서 전용 provenance 동기화로 진행한다 — task-0050 작성 + docs/master-plan.md의 stale 위치 동기화"`
