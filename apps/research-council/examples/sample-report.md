# Research Council Report: Sample

## Scope Notice

This sample report uses only user-provided input and deterministic placeholder
logic. No web search, network calls, LLM calls, or external evidence collection
were performed. No citations are generated.

## Input Summary

The user wants to validate whether a lightweight Research Council module is
useful enough to become the first practical Jarvis app.

## Claims

- `claim-001` (`user_provided`, medium): The raw idea and goal define a useful
  research direction.
- `claim-002` (`extracted`, low): The idea can be evaluated through a small
  experiment before broader integration.
- `claim-003` (`needs_evidence`, low): There is enough evidence to justify
  expanding Research Council beyond a local app module.

## Evidence Ledger

- `evidence-001` for `claim-001` (`provided`): The user supplied a concrete goal
  and requested a specific report workflow.
- `evidence-002` for `claim-002` (`missing`): No real user trial has been run yet.
- `evidence-003` for `claim-003` (`missing`): No adoption, quality, or usefulness
  evidence has been collected yet.

## Reviewer Critiques

- `critique-001` (`skeptic`, high): The strongest claims still depend on missing
  evidence.
- `critique-002` (`operator`, medium): The workflow should stay isolated until a
  smoke-tested local path exists.

## Minimum Viable Experiments

- `experiment-001`: Run one local end-to-end dry run with a real idea and inspect
  whether the generated report identifies useful next actions.

## Recommendation

Continue with a local minimum experiment before adding integrations.
